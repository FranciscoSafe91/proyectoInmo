{
  "name": "spyderconnect",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npx vite build && cd ../backend && npm install",
    "start": "node backend/src/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
