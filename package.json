{
  "name": "spyderconnect",
  "version": "1.0.0",
  "dependencies": {
    "cloudinary": "^2.5.1",
    "dotenv": "^16.4.5",
    "mysql2": "^3.11.0",
    "nodemailer": "^9.1.0"
  },
  "devDependencies": {
    "shx": "^0.3.4"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build && shx cp -r dist ../dist && shx rm -rf ../backend/public/app && shx cp -r dist ../backend/public/app",
    "postinstall": "npm run build",
    "start": "node backend/src/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
