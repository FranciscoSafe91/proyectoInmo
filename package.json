{
  "name": "spyderconnect",
  "version": "1.0.0",
  "dependencies": {
    "dotenv": "^16.4.5",
    "mysql2": "^3.11.0"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cp -r dist ../dist && rm -rf ../backend/public/app && cp -r dist ../backend/public/app",
    "postinstall": "npm run build",
    "start": "node backend/src/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
